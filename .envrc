# UPOS-7-VS Environment Configuration
# This file is automatically loaded by direnv when entering this directory
# Install direnv: https://direnv.net/docs/installation.html
# Then run: direnv allow

# OpenAI Configuration (Required for UPOS-7-VS)
# OpenAI API Key
OPENAI_API_KEY=

export OPENAI_MODEL="gpt-4o-mini"

# Next.js / Project Configuration (Optional)
# Uncomment and set as needed:
# export NEXT_PUBLIC_SITE_URL="${NEXT_PUBLIC_SITE_URL:-http://localhost:3000}"
# export NEXT_PUBLIC_SENTRY_DSN="${NEXT_PUBLIC_SENTRY_DSN:-}"

# Upstash Redis (If using rate limiting / caching)
# These are typically set via v0 integration, but can be added here:
# export KV_URL="${KV_URL:-}"
# export KV_REST_API_URL="${KV_REST_API_URL:-}"
# export KV_REST_API_TOKEN="${KV_REST_API_TOKEN:-}"
# export KV_REST_API_READ_ONLY_TOKEN="${KV_REST_API_READ_ONLY_TOKEN:-}"
# export REDIS_URL="${REDIS_URL:-}"

# GitHub Token (Optional - for higher API rate limits)
# export NEXT_PUBLIC_GITHUB_TOKEN="${NEXT_PUBLIC_GITHUB_TOKEN:-}"

